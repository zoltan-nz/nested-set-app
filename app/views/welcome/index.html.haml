%h1 Examples
.well
  %h4 Nested view with ul/li
  %ul
  = nested_li(@categories) do |c|
    =c.name


.well
  %h4 Select box with tree hierarchy
  %form.form
    =select_tag "categories", options_from_collection_for_select(@categories, "id", "name"), class: 'form-control'